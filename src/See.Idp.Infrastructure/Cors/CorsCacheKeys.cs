namespace See.Idp.Infrastructure.Cors;

/// <summary>Shared cache keys for CORS-related entries.</summary>
public static class CorsCacheKeys
{
    /// <summary>Cache key for the dynamically built CORS policy.</summary>
    public const string DynamicPolicy = "DynamicCorsPolicy";
}
